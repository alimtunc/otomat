# Generated by /turkit-init — detected pnpm, base main, Linear MCP present (needs auth) + OTO-N branch pattern
commands:
  dev: pnpm dev
  check: pnpm typecheck
  lint: pnpm lint
  fmt: pnpm format
  test: pnpm test
  build: pnpm build
base_branch: main
rules:
  docs:
    - AGENTS.md
workflow:
  token_budget: low
output:
  style: compact
  language: auto
  technical_terms: keep-english
