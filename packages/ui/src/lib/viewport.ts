/** Tailwind `lg`: below this panes stack into strips. */
export const WIDE_VIEWPORT_MEDIA_QUERY = "(min-width: 64rem)";

/** Tailwind `xl`: below this the shell falls back to the icon rail and side panels start collapsed. */
export const ROOMY_VIEWPORT_MEDIA_QUERY = "(min-width: 80rem)";
